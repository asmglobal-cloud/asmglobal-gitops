apiVersion: v1
kind: Namespace

metadata:
  name: education-risinggem-academy-dev

---

apiVersion: apps/v1
kind: Deployment

metadata:
  name: risinggem-academy
  namespace: education-risinggem-academy-dev

spec:
  replicas: 1

  selector:
    matchLabels:
      app: risinggem-academy

  template:
    metadata:
      labels:
        app: risinggem-academy

    spec:
      containers:

        - name: risinggem-academy

          image: knnrsolomon/education-os:release

          imagePullPolicy: Always

          ports:
            - containerPort: 80

          env:
            - name: TENANT_NAME
              value: "risinggem-academy"

            - name: TENANT_OS
              value: "education"

            - name: TENANT_ENV
              value: "dev"

---

apiVersion: v1
kind: Service

metadata:
  name: risinggem-academy
  namespace: education-risinggem-academy-dev

spec:
  selector:
    app: risinggem-academy

  ports:
    - port: 80
      targetPort: 80

---

apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: risinggem-academy
  namespace: education-risinggem-academy-dev

  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod

spec:
  ingressClassName: traefik

  rules:

    - host: risinggem-academy.education.asmglobal.cloud

      http:
        paths:

          - path: /
            pathType: Prefix

            backend:
              service:
                name: risinggem-academy
                port:
                  number: 80

  tls:
    - hosts:
        - risinggem-academy.education.asmglobal.cloud

      secretName: risinggem-academy-education-tls
