apiVersion: v1
kind: Namespace

metadata:
  name: education-glorybridge-academy-dev

---

apiVersion: apps/v1
kind: Deployment

metadata:
  name: glorybridge-academy
  namespace: education-glorybridge-academy-dev

spec:
  replicas: 0

  selector:
    matchLabels:
      app: glorybridge-academy

  template:
    metadata:
      labels:
        app: glorybridge-academy

    spec:
      containers:

        - name: glorybridge-academy

          image: knnrsolomon/education-os:release

          imagePullPolicy: Always

          ports:
            - containerPort: 80

          env:
            - name: TENANT_NAME
              value: "glorybridge-academy"

            - name: TENANT_OS
              value: "education"

            - name: TENANT_ENV
              value: "dev"

---

apiVersion: v1
kind: Service

metadata:
  name: glorybridge-academy
  namespace: education-glorybridge-academy-dev

spec:
  selector:
    app: glorybridge-academy

  ports:
    - port: 80
      targetPort: 80

---

apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: glorybridge-academy
  namespace: education-glorybridge-academy-dev

  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod

spec:
  ingressClassName: traefik

  rules:

    - host: glorybridge-academy.education.asmglobal.cloud

      http:
        paths:

          - path: /
            pathType: Prefix

            backend:
              service:
                name: glorybridge-academy
                port:
                  number: 80

  tls:
    - hosts:
        - glorybridge-academy.education.asmglobal.cloud

      secretName: glorybridge-academy-education-tls
