apiVersion: v1
kind: Namespace

metadata:
  name: education-covenant-academy-dev

---

apiVersion: apps/v1
kind: Deployment

metadata:
  name: covenant-academy
  namespace: education-covenant-academy-dev

spec:
  replicas: 1

  selector:
    matchLabels:
      app: covenant-academy

  template:
    metadata:
      labels:
        app: covenant-academy

    spec:
      containers:

        - name: covenant-academy

          image: knnrsolomon/education-os:release

          imagePullPolicy: Always

          ports:
            - containerPort: 80

          env:
            - name: TENANT_NAME
              value: "covenant-academy"

            - name: TENANT_OS
              value: "education"

            - name: TENANT_ENV
              value: "dev"

---

apiVersion: v1
kind: Service

metadata:
  name: covenant-academy
  namespace: education-covenant-academy-dev

spec:
  selector:
    app: covenant-academy

  ports:
    - port: 80
      targetPort: 80

---

apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: covenant-academy
  namespace: education-covenant-academy-dev

  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod

spec:
  ingressClassName: traefik

  rules:

    - host: covenant-academy.education.asmglobal.cloud

      http:
        paths:

          - path: /
            pathType: Prefix

            backend:
              service:
                name: covenant-academy
                port:
                  number: 80

  tls:
    - hosts:
        - covenant-academy.education.asmglobal.cloud

      secretName: covenant-academy-education-tls
