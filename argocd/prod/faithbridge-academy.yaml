apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: faithbridge-academy-prod

  namespace: argocd

spec:
  project: default

  source:
    repoURL: https://github.com/asmglobal-cloud/asmglobal-gitops.git

    targetRevision: release

    path: tenants/prod/education/faithbridge-academy

  destination:
    server: https://kubernetes.default.svc

    namespace: education-faithbridge-academy-prod

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true
