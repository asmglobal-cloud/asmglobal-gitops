apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: destiny-academy-dev

  namespace: argocd

spec:
  project: default

  source:
    repoURL: https://github.com/asmglobal-cloud/asmglobal-gitops.git

    targetRevision: release

    path: tenants/dev/education/destiny-academy

  destination:
    server: https://kubernetes.default.svc

    namespace: education-destiny-academy-dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true
