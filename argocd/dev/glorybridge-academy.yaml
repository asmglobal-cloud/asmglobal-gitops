apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: glorybridge-academy-dev

  namespace: argocd

spec:
  project: default

  source:
    repoURL: https://github.com/asmglobal-cloud/asmglobal-gitops.git

    targetRevision: release

    path: tenants/dev/education/glorybridge-academy

  destination:
    server: https://kubernetes.default.svc

    namespace: education-glorybridge-academy-dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true
