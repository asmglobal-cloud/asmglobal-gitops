apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: asmglobal-gitops-root

  namespace: argocd

spec:
  project: default

  source:
    repoURL: https://github.com/asmglobal-cloud/asmglobal-gitops.git

    targetRevision: release

    path: argocd

  destination:
    server: https://kubernetes.default.svc

    namespace: argocd

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
